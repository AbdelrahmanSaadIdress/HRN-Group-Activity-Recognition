# Same as RCRG_1R_1C.py without any changes.
# The difference is what you will pass as stage 1.
# Here You will not pass a finetuned model and just make stage one depend on the pretrained weights.
# !!!! Don't pass Checkpoint to be loaded for the first stage and just depend on the pretrained weights.